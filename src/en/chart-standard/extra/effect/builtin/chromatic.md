# `chromatic`

Chromatic aberration effect.

![Example](image/chromatic.png)

## Parameters

- `sampleCount` (int, default `3`, range 1–64): Number of samples; higher gives smoother result and higher cost.
- `power` (float, default `0.01`): Strength, i.e. offset distance.
