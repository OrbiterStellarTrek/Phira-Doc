# Coordinates

The origin is at the top-left of the first screen after you scroll down one screen on the event page. The x-axis goes right; the y-axis goes down.

Screen width is always `2`. Screen height can be derived from the aspect ratio; see [built-in variables](./variable.md#built-in-variables).
